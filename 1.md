# 音频处理与数据库存储功能说明

## 功能概述

本模块实现了从音频文件到数据库存储的完整处理流程，主要包括：
- 音频文件的转写处理
- 文本内容提取与向量化
- 智能摘要生成
- 向量化存储与数据库持久化

## 核心函数

### extract_text_from_transcripts

从转写结果中提取纯文本内容。

```python
def extract_text_from_transcripts(transcripts: List[Dict]) -> List[str]
```

**参数：**
- `transcripts` (List[Dict]): 转写结果列表，每个元素包含以下字段：
  - `start`: 开始时间
  - `end`: 结束时间
  - `speaker`: 说话人标识
  - `text`: 转写文本内容

**返回值：**
- `List[str]`: 纯文本内容列表，仅包含每个转写片段的 text 字段

**示例：**
```python
transcripts = [
    {"start": 0, "end": 5, "speaker": "A", "text": "大家好"},
    {"start": 5, "end": 10, "speaker": "B", "text": "欢迎参加本次会议"}
]
text_list = extract_text_from_transcripts(transcripts)
# 返回: ["大家好", "欢迎参加本次会议"]
```

## 主要处理流程

### 1. 媒体文件处理
- 使用 `process_media()` 对音频文件进行转写处理
- 输入：音频文件路径或URL
- 输出：结构化的转写结果

### 2. 文本提取
- 从转写结果中提取纯文本内容
- 过滤空文本，保留有效内容

### 3. 智能摘要生成
- 使用 `summary_analysis()` 对文本进行智能分析
- 参数：
  - `list_d`: 文本列表
  - `2`: 分段数量（可配置）
- 返回包含摘要和分段结果的 FutureSummary 对象

### 4. 向量化处理
- **摘要向量化**：对整个摘要内容进行向量化
- **分段向量化**：对每个文本段落分别进行向量化
- 使用 `get_embedding()` 生成向量表示

### 5. 数据库存储
- 使用 `RecordingDAO.add_document_with_paragraphs()` 存储处理结果
- 存储内容：
  - 文档标题
  - 原始转写数据
  - 文档摘要
  - 摘要向量
  - 音频文件URL
  - 分段文本及对应向量

## 性能监控

代码中集成了各步骤的执行时间统计：

| 步骤 | 监控内容 |
|------|----------|
| 媒体文件处理 | 音频转写处理耗时 |
| 文本提取 | 文本提取操作耗时 |
| 摘要分析 | 智能摘要生成耗时 |
| 摘要向量化 | 摘要向量生成耗时 |
| 分段向量化 | 分段向量生成耗时 |
| 数据库操作 | 数据持久化总耗时 |

## 依赖模块

```
- sql.dao.recording_document_dao: 数据库操作DAO
- sql.db: 数据库会话管理
- audioAnalysis.analysis_tool: 音频分析处理
- tool.embedding: 向量化工具
- tool.large_model: 大模型摘要分析
```

## 数据库表结构

假设 `RecordingDAO.add_document_with_paragraphs` 操作以下表：

### 主文档表
| 字段 | 类型 | 说明 |
|------|------|------|
| title | String | 文档标题 |
| content_json | JSON | 原始转写数据 |
| embedding | Vector | 摘要向量 |
| summary | Text | 文档摘要 |
| file_url | String | 音频文件URL |

### 段落表
| 字段 | 类型 | 说明 |
|------|------|------|
| embedding | Vector | 段落向量 |
| segment | Text | 段落文本内容 |

## 使用示例

```python
# 完整处理流程
title = "医药NLP项目"
url = "x.mp3"

# 1. 处理音频文件
data = process_media(url)

# 2. 提取文本
text_list = extract_text_from_transcripts(data)

# 3. 生成摘要
future_summary = summary_analysis(text_list, 2)

# 4. 向量化处理
summary_embedding = get_embedding(future_summary.summary)

# 5. 分段向量化
segments_data = []
for segment in future_summary.segments:
    seg_embedding = get_embedding(segment)
    segments_data.append({
        "embedding": seg_embedding,
        "segment": segment
    })

# 6. 存储到数据库
DBSession.init()
session = DBSession.get_session()
RecordingDAO.add_document_with_paragraphs(
    session,
    title=title,
    content_json=data,
    embedding=summary_embedding,
    summary=future_summary.summary,
    file_url=url,
    paragraphs=segments_data
)
```

